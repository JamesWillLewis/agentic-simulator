package com.simplesystemsim.agent;

import com.simplesystemsim.environment.Environment;

/**
 * A type of agent that uses generative AI (via an LLM) to make decisions.
 */
public class GenerativeAgent extends AbstractAgent {


    @Override
    public AgentOutput enact(AgentInput input) {
        return null;
    }
}
