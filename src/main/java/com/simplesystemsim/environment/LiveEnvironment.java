package com.simplesystemsim.environment;

/**
 * A live environment is a type of continuous (non-discrete) environment where each agent runs in a separate thread
 * and continuously responds to stimuli, as if in the real world. There is no concept of ticks.
 */
public class LiveEnvironment extends ContinuousEnvironment {
}
